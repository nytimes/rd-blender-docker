language: python
python:
  - "3.6"
sudo: required
services:
  - docker
script:
  - python3 generate.py
  - python3 build.py
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
after_success:
  - bash ./notify.sh