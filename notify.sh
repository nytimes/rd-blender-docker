#!/bin/bash
curl -X POST -H 'Content-type: application/json' --data '{"text":"Blender Docker images deployed to Docker Hub ":docker:" https://hub.docker.com/r/nytimes/rd-blender-docker", "username": "Blender Docker CI", "icon_emoji": ":blender-3d:"}' ${WEBHOOK_URL}